#include<bits/std++.h>
using namespace std;
bool check(){

	return ;
}
int Binary_Search(int l,int r){
	int lt=l,rt=r;
	for(int i=l;i<=r;i++){
		if(check(mid))lt=mid;
		else rt=mid;
		if(mid==n)return mid;
	}
}int main(){
	int n;
	cin>>n;
	Binary_Search(1,n);
	return 0;
}
